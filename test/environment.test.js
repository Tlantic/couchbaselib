"use strict";
const
	mock = require( 'couchbase' ).Mock,
	CouchBaseEnvironment = require( '../lib/environment' )( mock ),
	expect = require( 'chai' ).expect;


describe( 'Environment', function () {
	
	
});